---
# defaults file for debian_omv
# version of OpenMediaVault
# synchrony is not yet GA, so install will most likely fail
omv_version: synchrony
# omv_version: sandworm
omv_first_install: true
omv_arch: arm64
